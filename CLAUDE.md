# FilterMe

YouTube Live 시청자 개인용 AI 채팅 필터 Chrome Extension.

## 목표

YouTube 라이브 채팅을 실시간으로 분석하고,
사용자가 켠 필터 항목에 해당하는 채팅을 화면에서 숨긴다.

AI는 각 채팅을 여러 기준으로 분석해 항목별 점수를 매기고,
최종적으로 숨길지 여부는 사용자가 켠 항목과 임계값에 따라 클라이언트에서 결정한다.

## 필터 기준

### 1. 도배

* 같은 내용 반복
* 과도한 `ㅋㅋㅋㅋ`, `ㅎㅎㅎㅎ` 등 반복 문자
* 이모지 반복
* 짧은 시간 동안 동일하거나 유사한 채팅 반복

### 2. 욕설

* 욕설
* 비속어
* 수위가 높은 표현

### 3. 비방 / 공격

* 특정 사람을 향한 비난
* 시비성 표현
* 모욕적인 표현
* 공격적인 채팅

### 4. 광고 / 홍보

* 반복적인 홍보 문구
* 외부 사이트 또는 서비스 홍보
* 광고성 링크
* 방송과 관계없는 홍보성 채팅

## 분석 결과

AI는 각 채팅을 항목별로 분석해 0~100 점수를 매긴다.
점수가 높을수록 해당 유형에 가깝다는 의미다.

예시:

```json
{
  "spam": 82,
  "profanity": 15,
  "aggression": 63,
  "advertising": 4
}
```

## 사용자 필터 설정

사용자는 항목별 on/off 토글로 필터를 켠다.

* 도배
* 욕설
* 비방/공격
* 광고/홍보

켠 항목에 대해서만 필터가 동작한다. (예: 욕설만 켜면 욕설로 판정된 채팅만 숨김)

각 항목에는 고정 임계값이 있다(예: 70). 켠 항목의 점수가 임계값 이상이면
해당 채팅을 화면에서 숨긴다. 필터 판정은 클라이언트(확장 프로그램)에서 수행한다.

## 프로젝트 구조

* `extension/`: Chrome Extension
* `server/`: AI 분석 서버
* `docs/roadmap.md`: 단계별 구현 계획

## 기술

### Extension

* JavaScript
* Chrome Extension Manifest V3

### Server

* Python
* FastAPI
* 채팅 분석은 초기엔 규칙 기반 mock, 이후 군집화 기반 모델로 교체

## 통신

* YouTube → 서버: `liveChatMessages.streamList` (server-streaming, 실시간 push)
* 서버 ↔ 확장 프로그램: WebSocket (방송 단위 방)

## 기본 흐름

YouTube Live Chat
→ 서버가 streamList로 채팅 실시간 수신
→ 서버에서 AI가 채팅마다 항목별 점수 분석
→ 같은 방송(방)을 보는 확장 프로그램들에게 WebSocket으로 분석 결과 broadcast
→ 확장 프로그램이 화면의 채팅 DOM과 매칭
→ 사용자가 켠 항목의 점수가 임계값 이상이면 해당 채팅 숨김

## 개발 원칙

* 한 단계씩 구현한다.
* 처음부터 전체 기능을 한 번에 만들지 않는다.
* 기능별로 동작 확인 후 다음 단계로 진행한다.
* API Key 및 Secret은 서버에서만 관리한다.
* Extension 코드에 API Key나 Secret을 저장하지 않는다.
